.direnv/

# Publish output (publish.ps1 / desktop build scripts -> ./dist/<rid>/Server[.exe])
/dist/

node_modules/

# .NET build output. DesktopApp/bin is ~127M and must never be committed.
bin/
obj/
